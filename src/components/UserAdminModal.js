// Deprecated: UserAdminModal replaced by UserManagementModal
// O arquivo foi mantido como placeholder para evitar erros de import durante deploys antigos.
import React from 'react';

export default function DeprecatedUserAdminModal() {
  // Não renderiza mais nada. Use `UserManagementModal` em ./UserManagementModal.js
  return null;
}
